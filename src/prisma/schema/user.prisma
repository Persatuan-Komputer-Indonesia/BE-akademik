model User {
  id         String    @id @default(uuid())
  username   String
  email      String    @unique
  password   String
  created_at DateTime  @default(now())
  role       Role
  deletedAt  DateTime?
  Otp        Otp[]
  profile    Profile?
  otps       OTP[]
}
